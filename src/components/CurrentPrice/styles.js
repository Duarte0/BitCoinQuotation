import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    headerPrice: {
        width: "100%",
        height: "auto",
        alignItems: "center",
        marginBottom: 20
    },
    currentPrice: {
        color: "#b89e14",
        fontSize: 30,
        fontWeight: "bold",
        paddingTop: 20,
    }, 
    textPrice: {
        color: "white",
        fontSize: 15,
        paddingTop: 5,
        fontWeight: "bold"
    }
})

export default styles